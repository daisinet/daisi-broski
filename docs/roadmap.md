# Roadmap

> Phased delivery plan for daisi-broski. Each phase is independently shippable — you can stop at any phase and have a useful tool.
> [Architecture](architecture.md)

---

## Current state

**Phases 0, 1, 3a, 3b, 3c, and 4 are complete.** Phase 3 is feature-complete. BigInt (slice 3c-11) closed the original 3c roadmap; follow-up slices added regex literals (slice 3c-12) + the common browser host shims (slice 3c-13) after real-page testing showed those were the biggest blockers on the wild web. Phase 4 landed out of order — ahead of phases 2 and 3 — because a sandboxed phase-1 engine is immediately useful for scraping, link extraction, and preview generation, while phase 2 (CSSOM) is mostly plumbing that doesn't pay off until phase 3 is in. Phase 2 is effectively absorbed into phase 3c — the cascade and event dispatch work landed as they were needed by the DOM bridge and the event system. Next major milestone: phase 5 (sandbox hardening / cross-platform).

**Combined test suite: 1627/1627 passing.** Real-page ship gate: **every inline script runs cleanly on every modern-framework site tested** — svelte.dev 6/6, react.dev 3/3, **nextjs.org 50/50**, **tailwindcss.com 185/185**, **nodejs.org 118/118**, typescriptlang.org 5/5, vitejs.dev, preactjs.com, nuxt.com, remix.run, htmx.org, MDN, rust-lang.org, and more. ~375 real inline scripts executing across 13 real sites with zero runtime errors. Real-world compatibility on heavyweight bundle sites: stripe.com 44/68, anthropic.com 18/24, figma.com 89/108, linear.app 71/114, cloudflare.com 21/24.

What works today from a clean clone:

```
$ daisi-broski fetch https://news.ycombinator.com --select ".titleline > a"
200 OK https://news.ycombinator.com/ (35118 bytes, utf-8)
How to breathe in fewer microplastics in your home
... 29 more ...
30 match(es)
```

The full pipeline runs inside a `Daisi.Broski.Sandbox.exe` child process under a Win32 Job Object (256 MiB memory cap, kill-on-close, die-on-unhandled-exception, UI restrictions). `--no-sandbox` falls back to in-process execution.

---

## Phase 0 — Scaffolding ✅

- Repo created, .NET 10 conventions set up.
- Architecture and roadmap docs written.
- LICENSE, `.gitignore`, `global.json`, `Directory.Build.props` in place.

## Phase 1 — Network + HTML parser + DOM (no JS) ✅

**Goal:** fetch a URL, parse it, return a queryable DOM snapshot. No script execution.

**Ship gate achieved:**

```
$ daisi-broski fetch https://news.ycombinator.com --select ".titleline > a"
```

returns 30 story links, identical to what Chrome sees.

### Shipped

- **`Daisi.Broski.Engine`** core library:
  - `Net.HttpFetcher` — `HttpClient` facade with manual redirect following (capped), per-session `CookieContainer`, Chromium User-Agent default, streaming response size cap (50 MiB default). Decompression (gzip / deflate / brotli) via `SocketsHttpHandler`.
  - `Html.EncodingSniffer` — WHATWG encoding sniffing: BOM → `Content-Type` charset → `<meta>` prescan of the first 1024 bytes → UTF-8 fallback.
  - `Html.Tokenizer` — WHATWG HTML5 state machine, phase-1 subset: data, tag open/close/name, all three attribute value forms, self-closing, comments, DOCTYPE name, RAWTEXT / RCDATA / ScriptData for `<script>` / `<style>` / `<title>` / `<textarea>` / `<noscript>` / `<iframe>` / `<noembed>` / `<noframes>` / `<xmp>`.
  - `Html.HtmlEntities` — character-reference decoder: ~120 named entities covering >99% of real-world usage, decimal and hex numeric references, WHATWG Windows-1252 fixup for code points 0x80-0x9F, surrogate / out-of-range → U+FFFD.
  - `Html.HtmlTreeBuilder` — WHATWG insertion-mode state machine, phase-1 subset: Initial → BeforeHtml → BeforeHead → InHead → AfterHead → InBody → Text → AfterBody → AfterAfterBody. Implicit html/head/body synthesis, implicit `<p>` close on block-level elements, implicit close of same-named list / row / option / dd / dt, void element handling, character-run merging into single `Text` nodes, simplified "pop until matching name" for misnested end tags.
  - `Dom.{Node, Element, Document, Text, Comment, DocumentType}` — doubly-linked-list-backed tree with `ChildNodes`, sibling / parent pointers, `ownerDocument` adoption on attach, cycle-safe `AppendChild`, `getElementById` / `getElementsByTagName` / `getElementsByClassName`.
  - `Dom.Selectors.{SelectorParser, SelectorMatcher}` — CSS Selectors Level 4 pragmatic subset: type / universal / id / class / attribute (all 7 match operators + case-insensitive flag), compound, all four combinators, selector lists, pseudo-classes (`:first-child`, `:last-child`, `:only-child`, `:first-of-type`, `:last-of-type`, `:only-of-type`, `:nth-child(An+B)` and friends, `:root`, `:empty`, `:not`). Right-to-left matching via `SelectorMatcher.Matches`. Wired onto `Node.QuerySelector` / `Node.QuerySelectorAll`, `Element.Matches` / `Element.Closest`.
  - `PageLoader` — thin end-to-end glue: `HttpFetcher` → `EncodingSniffer` → `HtmlTreeBuilder` → `Document`.
- **`Daisi.Broski.Cli`** — command-line driver: `daisi-broski fetch <url> [--select <css>] [--html] [--ua <s>] [--max-redirects N] [--no-sandbox]`. Since phase 4, the default path spawns a sandbox child.

### Deliberately deferred (documented in the relevant class doc comments)

- **Tokenizer:** CDATA sections, DOCTYPE public/system identifiers, full script-data escape sub-states (legacy HTML3/IE compat), legacy no-semicolon named entities.
- **Tree builder:** table insertion modes (table tags parse as regular elements — wrong structure for malformed tables, works for well-formed ones), form element association, template elements, SVG/MathML foreign content, frameset, quirks mode, full adoption agency (misnested tag patterns like `<b><i></b></i>` give a different tree than Chrome).
- **DOM:** the original phase-1 sketch mentioned `Attr` and `NodeList` as distinct types. They were not implemented. Attributes are stored as an ordered `List<KeyValuePair<string, string>>` on `Element` (iteration order is preserved for future JS API compatibility), and node collections are exposed as `IReadOnlyList<Node>`. A proper `Attr` node and a live `NodeList` will be added in phase 3c when the JS DOM bridge needs them.
- **Selectors:** pseudo-elements (`::before`, `::after`), `:has` / `:is` / `:where`, `:hover` / `:focus` (no events / state), namespace prefixes.
- **Encoding:** `windows-1252`, `shift_jis`, `gbk`, and other legacy encodings aren't registered — unknown encoding names fall back to UTF-8 with mojibake. All modern web content is UTF-8.
- **html5lib conformance suite:** the original phase-1 plan called for vendoring the html5lib `.dat` test vectors and hitting >90% pass rate on the tokenizer and tree-construction suites. This was not done. Phase 1 ships with ~100 hand-written xUnit tests covering the same surface, which is sufficient for the ship-gate demo but is not an objective measure of spec conformance. Adding html5lib is a cheap follow-up — the tests are text files, no code needed to vendor them, only a test runner that iterates them.

### Design decisions captured

- [DD-01 Regex engine](design-decisions.md#dd-01--regex-engine) — BCL as phase-3a placeholder, hand-written NFA by 3c.
- [DD-05 JS heap and GC strategy](design-decisions.md#dd-05--js-heap-and-gc-strategy) — .NET GC in 3a/3b, refactor to tagged-union struct in 3c, decide arena vs. pool only after phase-7 profiling.

## Phase 2 — CSSOM + Web APIs (still no JS)

**Goal:** enough CSSOM and host-API plumbing that the JS engine has somewhere to attach.

- Full cascade: specificity, `!important`, inheritance, `var()`, `calc()`, media queries.
- `getComputedStyle` returning declared values (layout-dependent ones stubbed).
- Event dispatch system: `EventTarget`, `addEventListener`, bubbling/capturing, `CustomEvent`.
- Host API seams for the Web APIs that phase 3 will need (`setTimeout`, `console`, `fetch` — the C# side is ready, the JS side is stubbed).

**Ship gate:** unit tests for cascade + selector match pass against a reasonable WPT subset.

**Current status:** not started. Likely absorbed into the front of phase 3 — the cascade and event dispatch are useful only once scripts can query computed styles and dispatch events, so building them in isolation is plumbing for phase 3 with no independent payoff.

## Phase 3 — JavaScript engine

**This is the single largest phase.** See [js-engine.md](js-engine.md) (planned) for the detailed breakdown. Three sub-phases:

### Phase 3a — ES5 core ✅

- **Lexer** ✅ — `Daisi.Broski.Engine.Js.JsLexer` and `JsTokenKind` / `JsToken`. Recognizes every ES5 keyword, the ES2015+ future-reserved keywords, decimal + scientific + hex number literals, single- and double-quoted string literals with the common escapes (`\n`, `\t`, `\r`, `\b`, `\f`, `\v`, `\0`, `\'`, `\"`, `\\`, `\xXX`, `\uXXXX`, line continuations), line and block comments (skipped, not emitted), and all ES5 punctuators including greedy long matches (`>>>=`, `===`, etc.). Deferred: regex literals (context-sensitive, needs parser cooperation), template literals (ES2015 — phase 3b), BigInt literals (phase 3c), Unicode identifiers beyond ASCII. 43 tests passing.
- **Parser + AST** ✅ — `Daisi.Broski.Engine.Js.JsParser` and the ESTree-shaped sealed-class hierarchy in `Ast.cs`. Covers every ES5 statement form (`var`/`function`/`if`/`while`/`do..while`/C-style `for`/`for..in`/`break`/`continue`/`return`/`throw`/`try`/`catch`/`finally`/`switch` with fall-through/`with`/`debugger`/labeled/block/empty/expression) and every ES5 expression form including the full operator-precedence table, right-associative assignment and conditional, member access, computed member access, function calls, `new` with and without arguments, array and object literals (with holes, trailing commas, reserved-word keys, and `get`/`set` accessors), and function expressions (named and anonymous). Implements automatic semicolon insertion including the restricted productions (`return`/`throw`/`break`/`continue`, postfix `++`/`--`) and the `in`-operator ambiguity in `for` headers. `let`/`const` are accepted (tagged for future block scoping); other ES2015 forms (arrow, class, template, destructuring) are rejected with a descriptive error. Regex literals are still deferred. 69 tests passing.
- **Bytecode compiler + stack VM (slice 3 — primitives and global control flow)** ✅ — `Daisi.Broski.Engine.Js.JsCompiler` walks the parser's AST into a `Chunk` of single-byte opcodes and the `JsVM` executes it on a value stack. `JsEngine` wraps the pipeline into `Evaluate(source)`, returning the completion value of the last top-level expression per ECMA §14. Scope covers: primitive literals; `var` with hoisted global declarations; assignment and compound assignment to identifiers; every unary / binary / logical / conditional / sequence / update operator; `typeof` (special-cased for undeclared identifiers); `if`/`else`, `while`, `do..while`, C-style `for`, `break`, and `continue` (unlabeled); ES §11.9 loose and strict equality, §11.8.5 relational comparison with string-string and numeric coercion, §9.5 / §9.6 `ToInt32` / `ToUint32` for bitwise ops and shifts, and §9.8.1 number-to-string formatting. Values are boxed .NET objects (DD-05 option A). 52 end-to-end tests.
- **Objects + arrays + member access (slice 4a)** ✅ — `JsObject` (prototype-aware property bag with virtual `Get`/`Set`/`Has`/`Delete`) and `JsArray` (subclass with dense `List<object?>` storage, numeric-index routing, virtual `length`, and `Array.prototype.toString`-equivalent `Join` for string coercion). Compiler emits `CreateObject` / `CreateArray` / `InitProperty` / `GetProperty`(`Computed`) / `SetProperty`(`Computed`) / `DeleteProperty`(`Computed`) / `In` / `Dup2` / `StoreScratch` / `LoadScratch` opcodes for object and array literals, dot and computed member access (including on the left of assignment, compound assignment, prefix and postfix update, and `delete`), and the `in` operator. Object literal keys follow the parser's normalized form (identifier / string / number all landing as strings). Reference equality for object identity via `===`. Array holes are approximated as explicit `undefined` (documented deferral). 38 end-to-end tests including nested object literals, compound member assignment, postfix update on computed indices, `in` on objects and arrays, array length truncation, and a word-counter dictionary iteration.
- **Functions + closures + `this` + `new` + `instanceof` (slice 4b)** ✅ — `JsEnvironment` (Dictionary-backed binding record with parent reference for chain lookup), `JsFunctionTemplate` (immutable compiled body with param names), and `JsFunction` (subclass of `JsObject` carrying a template plus captured env; every user function gets a fresh `prototype` object with `constructor` pointing back). The VM carries a call-frame stack, a current `_env` pointer, and a `_this` slot; the existing `LoadGlobal` / `StoreGlobal` / `DeclareGlobal` / `DeleteGlobal` opcodes now walk the env chain. New opcodes: `MakeFunction`, `Call` / `New` (u8 argc), `Return`, `Instanceof`, `LoadThis`, `Swap`. The compiler grew a frame stack — nested function bodies compile into their own `Chunk` — and hoists both `var`s and function declarations to the top of the enclosing function. Method calls bind `this` to the object via a `Dup` / `GetProperty` / `Swap` sequence. `new` allocates a fresh instance, links its prototype to `F.prototype` read live, runs the constructor, and substitutes the constructor's return value only if it is an object (ECMA §13.2.2). `arguments` binds to a `JsArray` of the raw argument values. Host-installed native functions work via a `NativeImpl` delegate. 34 end-to-end tests.
- **Remaining control flow — `for..in`, `switch`, labeled break/continue (slice 4c)** ✅ — `ForInStart` / `ForInNext` opcodes and an internal `ForInIterator` that snapshots enumerable own + inherited string keys (dedup by `HashSet<string>`, skipping `length` on arrays per spec). `for..in` supports identifier and `var identifier` LHS (member LHS is rare enough to defer). `switch` uses a dispatch-table layout: duplicate discriminant for each case test, `StrictEq` + `JumpIfTrue` to the case's entry label; entry labels do `Pop` (of the still-on-stack discriminant) + `Jump` to the body label; bodies are concatenated so fall-through happens by adjacency; default entry skips the `Pop` because it is only reached via fall-through or the no-match path (which pops first). Labeled break / continue route through a generalized `BreakTarget` context stack — each loop or switch pushes one with an optional label, and `break`/`continue` walks the stack looking for the matching (labeled) target. `continue` can only target a loop (never a switch); labeled blocks accept `break label;` so `foo: { if (x) break foo; ... }` works. 25 end-to-end tests.
- **Exception handling — `throw` / `try` / `catch` / `finally` (slice 5)** ✅ — New opcodes `PushCatchHandler` / `PushFinallyHandler`, `PopHandler`, `Throw`, `EndFinally`, `PushEnv` / `PopEnv`. The VM carries a handler stack and a pending-exception slot. `DoThrow` unwinds handlers and call frames to find the nearest catch or finally; internal errors (`ReferenceError`, `TypeError`) are raised via a `RaiseError` helper so script-level `try`/`catch` intercepts them. Uncaught throws escape as a .NET `JsRuntimeException` carrying the thrown `JsValue`. Catch parameter gets its own block-scoped env via `PushEnv`. Three compile layouts for the three try shapes; catch+finally installs a finally-only handler around the catch body. 22 end-to-end tests. **Deferred**: cross-`finally` escape semantics for `return` / `break` / `continue`.
- **Built-in library, slice 6a — global functions + Array + String** ✅ — `JsEngine` now owns well-known prototype objects (`ObjectPrototype`, `ArrayPrototype`, `StringPrototype`, `NumberPrototype`, `BooleanPrototype`) and installs built-ins at construction. The VM takes a `JsEngine` reference so `CreateArray` sets the array prototype and `GetProperty` / `GetPropertyComputed` on a string primitive look up `length`, integer indices, and prototype methods. `JsObject.SetNonEnumerable` keeps prototype methods out of `for..in` enumeration. Native built-ins signal catchable JS exceptions via `JsThrow.TypeError` / etc., which throw an internal `JsThrowSignal` that `InvokeFunction` catches and routes through `DoThrow`. **Shipped globals**: `parseInt`, `parseFloat`, `isNaN`, `isFinite`. **Shipped Array**: `Array` constructor (length or elements), `Array.isArray`, and prototype methods `push`, `pop`, `shift`, `unshift`, `slice`, `concat`, `join`, `indexOf`, `reverse`, `sort` (no compareFn), `toString`. **Shipped String**: `String` coercion constructor, `String.fromCharCode`, and prototype methods `charAt`, `charCodeAt`, `indexOf`, `lastIndexOf`, `slice`, `substring`, `substr`, `toLowerCase`, `toUpperCase`, `trim`, `split`, `concat`, `toString`. String primitives get `length` and integer indexing via the VM's primitive-property lookup. 46 end-to-end tests including `'hello'.split('').reverse().join('')`.
- **Built-in library, slice 6b — re-entrant VM + Object + Math + Error + callback Array methods** ✅ — `JsVM.Run` is refactored into a reusable `RunLoop(stopFrameDepth)` plus a `_halted` flag; `JsVM.InvokeJsFunction(fn, this, args)` lets native built-ins run a JS function synchronously by setting up a nested dispatch against a new frame and running until the frame is popped. A `_nativeBoundaries` stack records the frame depth below which `DoThrow` cannot unwind without crossing the current native call; when it hits one, it escapes via a `JsThrowSignal` that the outer `InvokeFunction` catches and re-routes through `DoThrow` at the higher level, after cleaning up abandoned frames / handlers and restoring the saved VM state. Native built-ins that need the VM use a new `JsFunction.NativeCallable` delegate alongside the existing `NativeImpl`; `InvokeFunction` dispatches to whichever is set. **Array callback methods**: `forEach`, `map`, `filter`, `reduce`, `reduceRight`, `every`, `some`, and `sort` with a `compareFn`. **Object**: constructor, `Object.keys`, `Object.create`, `Object.getPrototypeOf`, and `Object.prototype.hasOwnProperty` / `isPrototypeOf` / `propertyIsEnumerable` / `toString` / `valueOf`. **Math**: `PI` / `E` / `LN2` / `LN10` / `LOG2E` / `LOG10E` / `SQRT2` / `SQRT1_2` plus `abs` / `ceil` / `floor` / `round` / `sqrt` / `exp` / `log` / `sin` / `cos` / `tan` / `asin` / `acos` / `atan` / `sign` / `pow` / `atan2` / `min` / `max` / `random`. **Error hierarchy**: `Error`, `TypeError`, `RangeError`, `SyntaxError`, `ReferenceError`, `EvalError`, `URIError`; each has a prototype carrying `name` / `message` / `toString`, and the subclass prototypes chain to `Error.prototype`. The VM's `RaiseError` now uses the engine's error prototypes so internal errors (reading an undeclared variable, calling a non-function) produce objects that satisfy `e instanceof ReferenceError` / `e instanceof TypeError` from script. 41 end-to-end tests including `filter`/`map`/`reduce` pipelines, numeric `sort(compareFn)`, callback throws propagating through `forEach` to an outer `try`/`catch`, `Math.min()` / `Math.max()` argument-less edge cases, and the VM-originated `instanceof TypeError` check.
- **Built-in library, slice 6c — JSON + Function.prototype + Number/Boolean** ✅ — `BuiltinJson` ships `JSON.parse` (recursive-descent parser over the full JSON grammar with string escapes including `\uXXXX`, surrogate handling via raw UTF-16, scientific notation, and proper error reporting via a `SyntaxError`) and `JSON.stringify` (cycle detection via a reference-equality `HashSet`, NaN/Infinity → `"null"`, functions/undefined omitted from objects and rendered as `null` inside arrays, compact output only — `replacer` / `space` arguments deferred). `BuiltinFunction` installs `Function.prototype.call`, `apply`, and `bind` as `NativeCallable` implementations that thunk through `JsVM.InvokeJsFunction`; `bind` creates a fresh `NativeCallable` closing over the target function, bound `this`, and any prefix arguments, and merges them with the runtime args on each call. `JsEngine.FunctionPrototype` is the new `[[Prototype]]` for every `JsFunction` value — set at `MakeFunction` time for user functions and fixed up post-install for built-ins via a prototype-chain walk at engine construction. `BuiltinNumberBoolean` ships `Number` constructor + static `MAX_VALUE` / `MIN_VALUE` / `NaN` / `POSITIVE_INFINITY` / `NEGATIVE_INFINITY` / `EPSILON` / `MAX_SAFE_INTEGER` / `MIN_SAFE_INTEGER`, `Number.isNaN` / `isFinite` / `isInteger` statics, and prototype `toString(radix)` / `toFixed` / `valueOf`. Boolean gets a coercion constructor and `toString` / `valueOf` prototype methods. 39 end-to-end tests including JSON roundtrips, cycle detection, `bind` composing with `forEach`, radix conversion, toFixed rounding, and Number statics.
- **Built-in library, slice 6d — `Date` (read-only subset)** ✅ — `JsDate : JsObject` with a `Time` slot storing milliseconds since Unix epoch (NaN for an invalid date). `Date()` constructor with three forms: no args (current time), numeric ms, component form (year, month, day?, hours?, minutes?, seconds?, ms?). `Date.now()` static. Read-only prototype methods: `getTime`/`valueOf`, local `getFullYear`/`getMonth`/`getDate`/`getDay`/`getHours`/`getMinutes`/`getSeconds`/`getMilliseconds`, UTC variants of all getters, `getTimezoneOffset`, `toISOString` (spec-exact format), `toJSON` (delegates to `toISOString`), `toString` (browser-style). `JsValue.ToNumber` and `ToJsString` special-case `JsDate` so `b - a` yields the ms difference and `'' + date` emits the ISO string. `JSON.stringify` special-cases `JsDate` so `JSON.stringify(new Date())` emits the ISO string without needing a VM-callback path through `toJSON`. 20 end-to-end tests including component constructor, UTC getters against a fixed epoch ms, date arithmetic, and JSON integration. Deferred: setters, `Date.parse` string parsing, `Date.UTC`, locale methods (`toLocaleString` etc.).
- **Event loop + `console` + timers (slice 7)** ✅ — `JsEngine` now owns a persistent `JsVM` that's reused across every `Evaluate` call and every event-loop task, so scheduled callbacks see bindings established by the main script. `JsVM.RunChunk(chunk)` replaces the old `Run()`, resetting `_ip`/`_sp`/frames/handlers while preserving the globals env. `RunLoop(stopFrameDepth)` was refactored so nested `InvokeJsFunction` calls don't mistakenly honor `_halted` from the outer script. **`JsEventLoop`**: task queue + microtask queue + sorted-by-due-time timer queue with a reverse `_timersById` index for O(log n) `clearTimer`. `Drain()` runs microtasks to completion, then dispatches one task, then checks timers (sleeping until the nearest due time when idle), and repeats until everything is empty or a 100,000-iteration safety limit trips. **`BuiltinConsole`**: `console.log` / `warn` / `error` / `info` / `debug` all append space-separated arguments to a `StringBuilder` on `JsEngine.ConsoleOutput` that hosts read directly. **`BuiltinTimers`**: `setTimeout` / `clearTimeout` / `setInterval` / `clearInterval` / `queueMicrotask` — all as native callables that thunk through the event loop. `setTimeout`/`setInterval` accept extra trailing arguments per the WHATWG spec and forward them to the callback. `JsEngine.RunScript` is a new convenience that calls `Evaluate` + `DrainEventLoop` in one step. Uncaught throws from event-loop tasks escape as `JsRuntimeException` via a catch in `DrainEventLoop` that converts the internal `JsThrowSignal`. 21 end-to-end tests covering console output, `queueMicrotask` FIFO, microtask-schedules-microtask, `setTimeout` with arg forwarding, `clearTimeout`, nested `setTimeout`, callbacks seeing persistent globals, actual wall-clock delay observance, `setInterval` with `clearInterval` from inside the callback, microtask drain between tasks, uncaught-throw host escape, and caught-inside-callback.
- Built-ins: `Object`, `Function`, `Array`, `String`, `Number`, `Boolean`, `Math`, `Date`, `RegExp`, `Error`, `JSON`, `arguments` (slice 6).
- ECMA regex engine (or BCL fallback — see [DD-01](design-decisions.md#dd-01--regex-engine)).
- Event loop (task queue + microtask queue, HTML spec semantics).
- Wire up `console`, `setTimeout`, `setInterval` as the first Web APIs backed by JS.
- Strict mode enforcement.

**Ship gate:** test262 ES5 subset >80% pass.

### Phase 3b — ES2015 core (in progress)

- **`let`/`const` with temporal dead zone + block scoping (slice 3b-1)** ✅ — new `JsUninitialized` sentinel and `DeclareLet` opcode; `BlockStatement` now pushes a fresh env, pre-scans the block for let/const and function declarations, and pops the env on exit. `LoadGlobal` / `LoadGlobalOrUndefined` both check for the sentinel and throw `ReferenceError` so `typeof` of a TDZ binding also throws per spec. For-loop `let` init wraps the whole loop in an env. Function declarations at block scope now hoist into the block env (not the enclosing function), so inner closures capture the block env and can read `let` bindings declared alongside them. Top-level `let` / `const` persist in the globals env across successive `Evaluate` calls (pragmatic REPL-friendly deviation from the spec's module record). Per-iteration freshness for `for (let i ...)` is deferred. 21 end-to-end tests.
- **Arrow functions (slice 3b-2)** ✅ — `ArrowFunctionExpression` AST node. Parser's `ParseAssignmentExpression` peeks for `Identifier =>` and `(...) => body` via a read-only forward scan for the matching close paren; falls through to the normal expression parse when nothing follows the `)`. `JsFunctionTemplate.IsArrow` and `JsFunction.CapturedThis`: the VM snapshots the current `_this` at `MakeFunction` time and the call path uses it instead of the caller's thisVal. Arrow call setup skips binding a fresh `arguments` object, so references resolve up the env chain to the enclosing function's. `DoNew` rejects arrow targets with a `TypeError`. Concise body `x => expr` is lowered to `{ return expr; }` via a synthetic `ReturnStatement` wrapper so it reuses the normal function-body compile path. 21 end-to-end tests including currying, method-with-inner-arrow `this` preservation, `arguments` inheritance from outer, arrows passed to `forEach`/`map`/`filter`/`reduce`, and `new` rejection.
- **Template literals (slice 3b-3)** ✅ — Four new token kinds (`NoSubstitutionTemplate`, `TemplateHead`, `TemplateMiddle`, `TemplateTail`) and a lexer state machine. The lexer tracks `_braceDepth` for `{`/`}` nesting and a `_templateStack` of the brace depths at each active `${` opening; when a `}` is seen at the top-of-stack's matching depth, the lexer switches back to template-string scan mode instead of emitting a `RightBrace` punctuator. This handles nested interpolations (`` `a${`b${c}d`}e` ``) and object literals inside interpolations (`` `${ {a: 1}.a }` ``). Template-literal escape sequences include the usual `\n`/`\t`/etc. plus `\`` and `\$`, and CRLF line endings normalize to LF. `TemplateLiteral` AST node with quasis (decoded string parts) and expressions. The compiler lowers template literals to a sequence of `PushConst` + `Add` operations that reuse the existing string-concat behavior of the `+` operator. 25 end-to-end tests including multi-line, nested templates, method calls inside interpolations, JSON.stringify round-trips, and arrows-plus-templates composition.
- **Destructuring bindings (slice 3b-4)** ✅ — `ObjectPattern` / `ObjectPatternProperty` / `ArrayPattern` / `ArrayPatternElement` AST nodes, and `VariableDeclarator.Id` is now a `JsNode` so it can hold an `Identifier`, `ObjectPattern`, or `ArrayPattern`. Parser grew `ParseBindingTarget` which dispatches on the first token; patterns are only accepted in declaration binding positions in this slice, and a destructuring declaration without an initializer is a syntax error per spec. The compiler lowers patterns to a purely compile-time walk: object patterns emit `Dup` / `GetProperty` / recursive binding / `Pop`; array patterns emit `Dup` / `PushConst(index)` / `GetPropertyComputed` / recursive binding / `Pop`. Elisions in array patterns are honored as skipped slots. Defaults (`{a = 1}` / `[a = 1]`) are lazy — emitted as a `Dup` / `PushUndefined` / `StrictEq` / `JumpIfFalse` test over the extracted value, replacing it with the default expression only when strictly equal to `undefined`. A `CollectPatternNames` helper routes all hoisting sites (top-level let/const, block-scoped let/const, var hoist, for-loop init) through the same pattern-walk so each bound name gets its `DeclareGlobal` / `DeclareLet` opcode before the initializer runs. Deferred: function parameter destructuring, assignment-expression destructuring (non-declaration LHS), destructuring inside `for..in` / `for..of` heads. 38 end-to-end tests covering shorthand, rename, defaults (present / missing / null vs undefined / lazy evaluation / full expressions), nested patterns, array elisions, let/const patterns inside blocks, and realistic usage (pluck config fields, destructure `JSON.parse` results).
- **Default params + rest params + spread (slice 3b-5)** ✅ — New `FunctionParameter` AST node (target + default + `IsRest`) replaces the flat `IReadOnlyList<Identifier>` on `FunctionDeclaration` / `FunctionExpression` / `ArrowFunctionExpression`. Parser grew `ParseFunctionParameter` (defaults via `= expr`, rest via leading `...`, rest-must-be-last enforced at parse time) and a `ParseArgumentOrSpread` helper shared by call arguments. Array literal parser accepts `...expr` as an element, producing a new `SpreadElement` expression node. Array pattern parser accepts `...target` as a rest element (enforced last, no defaults). **Defaults**: the compiler emits a per-param check at function entry — `LoadGlobal` / `PushUndefined` / `StrictEq` / `JumpIfFalse` — that evaluates the default expression lazily and stores it into the binding. Defaults close over the enclosing scope and can see earlier params because those bindings are already in the function env by the time the entry code runs. **Rest parameters**: `JsFunctionTemplate` gained a `RestParamIndex` field; `JsVM.InvokeFunction` binds the positional params up to that index and then materializes a fresh `JsArray` (with `Array.prototype`) containing the leftover args, binding it to the rest name. The rest array participates in `reduce`/`map`/`join` like any normal array. **Spread**: two new opcodes `ArrayAppend` (append one value to a TOS array) and `ArrayAppendSpread` (flatten another `JsArray` into a TOS array), and `CallSpread` / `NewSpread` that pop a pre-built args array and dispatch through the existing `InvokeFunction` path. The array literal compiler uses the fast `CreateArray n` path when there are no spreads and switches to the append-builder path when any element is a spread; call and new sites follow the same fast/slow split. `EmitSpreadArgsArray` is shared between calls and `new`. **Array pattern rest** lowers to a compile-time call to `src.slice(i)` (reusing the existing `Call 1` opcode) so no new VM opcode is needed. 42 end-to-end tests across default-param edge cases (present / missing / undefined / null / lazy / prior-param reference / arrows / function expressions), rest params (collection, empty, forwarding through reduce/map, arrow arrows, rest-not-last syntax errors), array-pattern rest (tail capture, exhausted, entire-array, nested destructuring of tail), array-literal spread (whole / prefix-suffix / multiple sources / empty source / fresh-copy semantics), call-site spread (all-args / mixed / method call preserves `this` / receiving-side rest / combining with defaults / `new` construction), and combined real-world idioms. Deferred: function parameter destructuring, `...rest` in object patterns (ES2018), `...obj` spread in object literals (ES2018), iterator-protocol-driven spread (will ship with `for..of`).
- **Classes (slice 3b-6)** ✅ — ES2015 class declarations and class expressions, including `extends`, `super(args)` constructor calls, `super.method(args)` method calls from instance / constructor / static methods, static methods, default constructor synthesis (for extending classes, a pass-through `constructor(...args){super(...args)}`; for non-extending, an empty body), multi-level inheritance chains, and block-scoped class bindings (TDZ on hoist). New AST nodes `ClassDeclaration` / `ClassExpression` / `ClassBody` / `MethodDefinition` / `Super`; new parser entry `ParseClassDeclaration` and `KeywordClass` / `KeywordSuper` primary expression paths. **Five new opcodes** — `SetupSubclass` (links `subclass.[[Prototype]] = parent` and `subclass.prototype.[[Prototype]] = parent.prototype`), `InstallMethod` (installs a non-enumerable method on `class.prototype` and copies `class.prototype.[[Prototype]]` into the method's `HomeSuper`), `InstallStaticMethod` (same on the class itself, using `class.[[Prototype]]` for HomeSuper), `LinkConstructorSuper` (copies `class.prototype.[[Prototype]]` into the constructor function's HomeSuper), and `LoadSuper` (reads the currently executing function's HomeSuper). `JsFunction` gained a `HomeSuper` slot; `JsVM` tracks `_currentFn` on every call frame so `LoadSuper` can read the active method's home-super in O(1). The compiler's `CompileClassAssembly` emits the full sequence: evaluate parent, materialize constructor, `SetupSubclass` + `LinkConstructorSuper` (only if extending), then a `Dup`-driven loop installing each method with `InstallMethod` / `InstallStaticMethod`, and finally `Pop` to leave one class value on TOS. `CompileCall` intercepts `super(args)` (pops `HomeSuper.constructor` and calls with `this` rebound) and `super.foo(args)` (pops `HomeSuper[foo]` and calls with `this` rebound). 28 end-to-end tests including constructor field-store, instance method chaining, static methods, `instanceof` on instances and subclasses, `super.method()` via a three-level inheritance chain (A → B → C, where B extends A and C extends B), static method inheritance through `extends`, super-method with argument forwarding, default subclass constructor argument forwarding via rest-spread, named and anonymous class expressions, block-scoped `class` inside `{}`, realistic Animal/Mammal/Dog override chain with `super.describe()` + `super.type()` interplay, and compile-time error on duplicate constructors. Deferred: getter / setter accessor methods, private fields, static fields, computed method names, `new.target`, class expression self-reference via the optional inner name.
- **Iterators + `for..of` + `Symbol.iterator` (slice 3b-7a)** ✅ — Minimum-viable `Symbol` primitive: new `JsSymbol` internal class for identity-distinct values, symbol-keyed property bag on `JsObject` via a lazy `Dictionary<JsSymbol, object?>`, and a `BuiltinSymbol` factory that exposes `Symbol('desc')` and the well-known `Symbol.iterator`. `GetPropertyComputed` / `SetPropertyComputed` route symbol keys to the symbol bag instead of coercing to strings, so `obj[Symbol.iterator] = fn` and `obj[Symbol.iterator]` work from script. `Array.prototype[Symbol.iterator]` and `String.prototype[Symbol.iterator]` install iterator-factory methods that capture the source and return a fresh iterator object with a native `next()` that yields `{value, done}` per spec. New `ForOfStatement` AST node, parser for `for (x of iter)`, `for (var x of iter)`, `for (let x of iter)` and `for (const x of iter)` (treating `of` as a contextual keyword). Two new opcodes: **`ForOfStart`** (pops the iterable, calls its `[Symbol.iterator]()`, pushes the returned iterator) and **`ForOfNext`** (peeks the iterator, calls `next()` via the re-entrant VM dispatch, reads `done`, pushes `value` or pops the iterator and jumps past the loop). The VM grew `GetIteratorFromIterable` (resolves the iterator from a value, special-casing strings via `StringPrototype` to avoid boxing) and `AppendFromIterator` (drives the protocol for array-literal spread). `ArrayAppendSpread` now takes a fast path for `JsArray` sources and a slow iterator-protocol path for everything else — `[...customIterable]` and `f(...customIterable)` work with any object that implements `Symbol.iterator`. 24 end-to-end tests covering Symbol identity and description, symbol-keyed property storage and `for..in` exclusion, `for..of` over arrays / strings / custom iterables / a class-based custom iterable, empty arrays, `break` / `continue`, nested `for..of`, `var` / `let` / `const` / bare-identifier heads, spread over custom iterables and strings, and the "non-iterable throws TypeError" path.
- **Generators (slice 3b-7b)** ✅ — ES2015 `function*` declarations and expressions, `yield` expression (optionally with argument), and generator-as-iterator integration. Each generator owns a **private `JsVM` instance** so its bytecode state (IP, value stack, call frames, environment, handlers) can suspend at a yield and resume on the next `gen.next()` call without disturbing whatever else the engine's main VM is doing. The compiler emits `yield arg` as `CompileExpression(arg); YieldValue; YieldResume`; `YieldValue` pops the argument, stashes it in the VM's `YieldedValue` slot, and sets `_halted=true` to exit `RunLoop`, while `YieldResume` is the opcode that executes at resume time and pushes the most recent `YieldSentValue` onto the stack as the value of the yield expression. `InvokeFunction` short-circuits for generator templates (detected via `JsFunctionTemplate.IsGenerator`) and returns a fresh `JsGenerator` instance instead of running the body — a `_startingGenerator` bypass flag disables the shortcut exactly once, inside `StartGeneratorExecution`, so the real body can actually be wired up. The generator's "caller" in its private VM is a **sentinel call frame** whose code is a reusable one-byte chunk containing just `Halt`: when the generator body's final `Return` restores that sentinel's state, the next dispatch step runs `Halt`, which exits `RunLoop` cleanly and signals completion. `JsGenerator` installs `next()` as a non-enumerable native-callable method that closure-captures the generator instance (so `var n = gen.next; n()` still works), and `[Symbol.iterator]` returning `this` so the generator is its own iterator. 21 end-to-end tests: basic yield / return / done, sent values through `gen.next(arg)`, generator local state and closures, iterator-protocol integration (`for..of`, spread, `Symbol.iterator` identity), infinite generators consumed lazily, fibonacci, ping-pong send pattern, and syntax errors for `yield` outside a generator body or inside a nested non-generator function. Deferred: `yield*` delegation, `gen.return()` / `gen.throw()`, async generators, method-shorthand generators (`class { *m() {} }`), arrow generators.
- **`Map` / `Set` / `WeakMap` / `WeakSet` (slice 3b-8)** ✅ — Four new backing classes (`JsMap`, `JsSet`, `JsWeakMap`, `JsWeakSet`) plus a `SameValueZeroComparer` that implements the ES2015 `SameValueZero` algorithm — identical to `===` except `NaN` is equal to itself, so a map can actually use `NaN` as a key. **`Map`** is backed by a `Dictionary<object, object?>` keyed with the SameValueZero comparer; .NET's dictionary preserves insertion order in enumeration, matching the spec requirement that `Map` iterate in insertion order. Prototype ships `get` / `set` (chainable) / `has` / `delete` / `clear` / `forEach(cb)` plus the iterator-protocol methods `keys()` / `values()` / `entries()` and `[Symbol.iterator]` (which defaults to `entries()` per spec). The `.size` property is intercepted in `JsMap.Get(string)` so it always reflects the current entry count without needing real getter descriptors. **`Set`** uses a `Dictionary<object, byte>` (rather than `HashSet`, because `HashSet` does not guarantee enumeration order). Its prototype ships `add(v)` (chainable) / `has` / `delete` / `clear` / `forEach` plus the same four iterator helpers; for `Set`, `[Symbol.iterator]` defaults to `values()`, and `entries()` yields `[v, v]` pairs per spec quirk. **`WeakMap`** and **`WeakSet`** accept only object keys / values and throw `TypeError` on primitives; internally they use regular .NET collections keyed by `ReferenceEqualityComparer.Instance` — the "weak" part is not observable in our runtime since we don't expose GC to JS anyway, and can be layered later via `ConditionalWeakTable` without breaking user code. All four constructors accept an optional iterable of initializer entries (pairs for `Map` / `WeakMap`, values for `Set` / `WeakSet`) that populates the collection via the slice 3b-7a iterator protocol through `JsVM.GetIteratorFromIterable`. `forEach` snapshots entries before iterating so concurrent mutation during iteration doesn't crash .NET's dictionary enumerator (a documented deferral from full live-iteration semantics). 35 end-to-end tests including basic CRUD on all four collection types, `.size`, `SameValueZero` semantics (NaN key retrieval, +0/-0 collision), object identity for `Map` keys, insertion-order iteration, `for..of` and spread integration, Set deduplication through spread, chainable `.add()` / `.set()`, construction from iterables, word-count via `Map`, set intersection via `filter`, and typed rejection of primitive keys/values on the Weak variants.
- **`Promise` (slice 3b-9)** ✅ — New `JsPromise` class with a three-state `Pending → Fulfilled | Rejected` life cycle, reusing the existing event loop's microtask queue for spec-correct "run `.then` callbacks after the current script but before the next timer" ordering. `JsEngine.PromisePrototype` is the shared prototype. `BuiltinPromise` installs the `Promise` global with: `new Promise(executor)` (executor runs synchronously; a throw from it rejects the new promise), `.then(onFulfilled, onRejected)` and `.catch(onRejected)` on the prototype (both return a fresh chained promise whose settled state depends on the handler's return/throw), `Promise.resolve(v)` (short-circuits when `v` is already a promise), `Promise.reject(r)`, `Promise.all(iterable)` (fulfills with an in-order results array when every input fulfills, rejects on the first rejection; consumes the input via the slice 3b-7a iterator protocol), and `Promise.race(iterable)` (settles with whichever input settles first). Thenable adoption is implemented in `JsPromise.Resolve` — if the value is a generic object with a callable `.then`, the promise subscribes to it as if it were a promise. Handler exceptions (both `JsThrowSignal` and `JsRuntimeException`) from user handlers are caught and routed to the chained promise's `Reject`. Cycle detection: resolving a promise with itself rejects it with a `TypeError`. 27 end-to-end tests including sync + async ordering (classic "after" log before the `.then` body), chain forwarding, promise-returning handlers, error propagation through unhandled `.then`s, recovery via `.catch` + continued chain, `Promise.all` / `Promise.race` with mixes of promises and plain values, empty `Promise.all([])`, microtask-vs-timer ordering (microtasks drain before the next `setTimeout`), and thenable adoption. Deferred: `Promise.allSettled`, `Promise.any`, `Promise.prototype.finally`, unhandled-rejection host notification.
- **`async` / `await` (slice 3b-10)** ✅ — ES2017 async functions in all four declaration forms: `async function f()`, `async function () {}`, `async (params) => body`, and `async x => body`. Also `async method()` in class bodies. Parser tracks `_inAsyncBody` across nested function bodies so `await` is accepted only directly inside an async body (a plain function nested inside an async function resets the flag). `async` and `await` are both contextual keywords (lexed as plain identifiers); the parser detects them by string value plus no-LineTerminator + appropriate-following-token lookahead. **Lowering strategy**: async functions compile exactly like generators — `await expr` emits the same `YieldValue` + `YieldResume` pair that `yield` uses, and the resulting bytecode is flagged `IsAsync` on `JsFunctionTemplate`. At call time, `JsVM.InvokeFunction` short-circuits for async templates: it creates a `JsGenerator` (reusing slice 3b-7b's private-VM + sentinel-frame infrastructure) and an outer `JsPromise`, then hands them to a new `DriveAsyncGenerator` stepper that walks the generator one yield at a time, wrapping each yielded value in a resolved-promise if needed and subscribing with `.then(onFulfilled, onRejected)`. The on-fulfilled path resumes the generator normally with the resolved value; the on-rejected path resumes in **throw mode** so the rejection surfaces as a thrown exception at the `await` site — a new `YieldResumeWithThrow` flag on `JsVM` makes `YieldResume` dispatch `DoThrow` at the yield point instead of pushing the sent value, which routes the rejection through any surrounding `try`/`catch`. When the generator body returns, the outer promise is resolved with the return value (going through `JsPromise.Resolve`'s normal thenable-adoption path, so `return anotherPromise` chains correctly). An uncaught throw inside the async body rejects the outer promise. 25 end-to-end tests including async-returns-promise, throw-rejects-promise, sequential awaits, `try/catch` around an `await Promise.reject()`, async arrows (both paren and single-identifier), async-in-class-method, reduce-via-await over an array, parallel via `await Promise.all`, and classic "`await` defers the rest of the body to a microtask" ordering. Deferred: `for-await-of`, `async function*`, top-level await.
- **Typed arrays + `ArrayBuffer` + `DataView` (slice 3b-11)** ✅ — Four new backing classes: `JsArrayBuffer` (wraps a `byte[]` and exposes `.byteLength`), `JsTypedArray` (element-kind enum + buffer + byte offset + length, with `Get` / `Set` overrides routing canonical integer keys to `ReadElement` / `WriteElement` that use `BitConverter` for multi-byte reads and manual byte-level writes for the integer kinds), and `JsDataView` (explicit-endian byte access with `ReadBytes` / `WriteBytes` that reverse the byte order when the caller asks for big-endian). `TypedArrayKind` enum covers `Int8`, `Uint8`, `Uint8Clamped`, `Int16`, `Uint16`, `Int32`, `Uint32`, `Float32`, `Float64`. `Uint8ClampedArray` uses `Math.Round(d, ToEven)` with the spec's clamp rule (NaN → 0, negatives → 0, ≥255 → 255). `BuiltinTypedArrays.Install` wires up `ArrayBuffer`, the nine typed array globals, and `DataView`; each typed-array constructor supports the three spec forms (`new T(length)`, `new T(iterable-or-array-like)`, `new T(buffer, byteOffset?, length?)`), with the iterable form consuming any ES2015 iterable via `JsVM.GetIteratorFromIterable` and the array-like form falling back to `length` + integer-indexed reads. Each typed-array prototype exposes `set(source, offset?)` (array / typed-array / iterable), `subarray(start, end)` (aliases bytes), `slice(start, end)` (copies into a fresh buffer), `fill(value, start, end)`, `indexOf`, `join`, `toString`, `forEach` / `map` / `reduce` (callback-taking, re-entrant via `NativeCallable`), `values()`, and `[Symbol.iterator]`. `DataView` ships the full `getInt8` / `setInt8` / `getUint8` / `setUint8` / `getInt16` / `setInt16` / `getUint16` / `setUint16` / `getInt32` / `setInt32` / `getUint32` / `setUint32` / `getFloat32` / `setFloat32` / `getFloat64` / `setFloat64` surface with the usual `littleEndian?` final argument (big-endian default, matching spec). 38 end-to-end tests including `ArrayBuffer` construction + slice, round-tripping values through every element kind, the `Uint8ClampedArray` clamp-and-round-half-to-even behavior, `subarray` aliasing shared bytes vs. `slice` copying, `set` from three source shapes, `fill` with bounded range, `indexOf`, `for..of` / spread iteration, `forEach` / `map` / `reduce` over typed arrays, and `DataView` cross-endian round-trips verified against a `Uint8Array` view over the same buffer. Deferred: `BigInt64Array`, `BigUint64Array` (no BigInt yet), `SharedArrayBuffer`, atomics, and `TypedArray.from` / `TypedArray.of` statics.
- **ESM module loader (slice 3b-12)** ✅ — New AST nodes `ImportDeclaration` / `ImportSpecifier` / `ExportNamedDeclaration` / `ExportDefaultDeclaration` / `ExportSpecifier`. Parser accepts `import`/`export` only at source-element (top-level) level; supports all the common forms: default import, named import list (`{a, b as c}`), namespace import (`* as ns`), side-effect-only import, `export const/let/var/function/class`, `export { a, b as c }`, `export default <expr | function | class | named function | named class>`. `JsEngine.ModuleResolver` is a host-pluggable delegate that takes a specifier and referrer URL and returns a resolved URL + source. `JsEngine.ImportModule(specifier, referrer?)` is the top-level loader: it resolves the specifier, checks the cache (keyed by URL), parses the source, recursively loads every import declaration's target, collects the resolved imports into a binding map, compiles the program via the new `JsCompiler.CompileModule` path (which **defers named-export stores to the end of the body** so `export var n = 0; n++;` reflects the final value), and dispatches the chunk through `JsVM.RunModuleChunk`. `RunModuleChunk` temporarily swaps in a fresh module-local `JsEnvironment` chained above the engine's globals, pre-seeds it with the resolved imports plus a synthetic `$exports` binding pointing at the module's `Exports` JsObject, runs the chunk to completion via `RunLoop`, and restores the caller's env on exit. Modules are cached by resolved URL so multiple imports of the same specifier share the same `JsModule` instance and don't re-run the body. 19 end-to-end tests covering named / default / namespace / rename / side-effect imports and all the export forms, transitive three-level imports, module caching (single evaluation, shared references), class exports with `new`, integration with `Promise`, and error paths. Deferred: dynamic `import()`, re-exports (`export { a } from './b'`, `export * from`), live-binding semantics (we snapshot at end-of-module), top-level `await`, and `import.meta`.

**Ship gate:** test262 ES2015 subset >70% pass, plus a real React app renders its initial content.

### Phase 3c — ES2020+ sugar, DOM bridge, and web platform ✅

All 11 slices have landed. Phase 3 is feature-complete.

- **Real-accessor RegExp prototype + recursion guard + scope fixes (slice 3c-18)** ✅ — Surfaced by sweeping 10 fresh real sites (stripe, mongodb, notion, anthropic, cloudflare, ...) and capturing the script error patterns. Three failure modes dominated:
  1. `hasIndices is not a function` on stripe / mongodb / notion polyfills — root cause was the inner `for (var a in obj)` in core-js's regex-polyfill module clobbering an outer-function helper named `a`. Our `for-in` `var` head wasn't getting hoisted to function scope, so the per-iteration `StoreGlobal` walked up the env chain and overwrote the outer `a` with the iterated key. Fix: `CompileForIn` now emits a `DeclareGlobal` (which declares-in-current-env) for `var` heads right before the loop runs, plus a tracking `case ForInStatement` in `HoistInStatement`. The same polyfill also reads `Object.getOwnPropertyDescriptor(RegExp.prototype, 'flags').get.call(testObj)` to detect whether the native getter exists — we previously installed `flags` / `hasIndices` / `dotAll` / `global` / `ignoreCase` / `multiline` / `sticky` / `unicode` / `source` as data properties, so the polyfill reinstalled its own (broken) accessor over the top. Fix: install them as real accessor descriptors via `JsObject.SetAccessor`, expose `OwnAccessors` + `TryGetOwnAccessor` + `HasOwn` on `JsObject`, teach `Object.getOwnPropertyDescriptor` to return `{get, set, enumerable, configurable}` shape for accessor properties, and add `RegExpPrototype` to the engine's bootstrap walker so accessor functions get the canonical `Function.prototype` link.
  2. **Stack overflow in stripe + the test suite** — once the for-in scope fix exposed paths the polyfill couldn't reach before, runaway recursion through accessor / call cycles started crashing the host. Fix: `JsVM.InvokeFunction` now caps `_frames.Count` at 1024 and `JsVM.InvokeJsFunction` caps `_reentryDepth` at 100, both raising a script-visible `RangeError` instead of a CLR `StackOverflowException`. `RunChunk` catches `JsThrowSignal` at the top and converts to `JsRuntimeException` so uncaught throws surface to host code consistently.
  3. **Object.prototype.toString tags** — polyfills routinely sniff via `Object.prototype.toString.call(x).slice(8,-1) === 'RegExp'` and install reduced fallback paths when that returns `'Object'`. Fix: `BuiltinObject.ToStringMethod` now returns the spec-shaped `[object RegExp]` / `[object Date]` / `[object String]` / `[object Number]` / `[object Boolean]` tags in addition to the existing Array / Function ones.
  4. **Parser smaller wins**: object literal shorthand methods literally named `get` / `set` (e.g. `{ get(r){}, set(e){} }`) parse correctly, computed-key class accessors (`class { get [sym](){} }`) parse, and Unicode identifier characters (`{ ã: 'a', ñ: 'b' }`) lex as identifiers via `char.IsLetter`. 9 new accessor / scoping tests. Combined suite: 1588 → 1627 passing. Real-page impact: stripe.com no longer stack-overflows; mongodb 4→5/13, anthropic 17→18/24, notion 13→14/26.
- **self / globalThis aliases → unlocks every Next.js site (slice 3c-17)** ✅ — A one-line alias install (`Globals["self"] = Globals["window"]` and same for `globalThis`) unblocks every Next.js / Nuxt / bundler-streamed-SSR site in one shot. Before: every inline chunk calling `self.__next_f.push(...)` failed with `ReferenceError: self is not defined` — that's ~185 failing scripts on tailwindcss.com alone. After: **nextjs.org 50/50 inline scripts clean** (and Next.js hydration actually processes the streamed RSC data, reducing the parsed DOM from 2219 → 507 elements as suspense boundaries resolve), **tailwindcss.com 185/185**, **nodejs.org 118/118**. Also added a broader CLI sweep counter so the `run` command reports `N total (X inline, Y external, Z other type)` + `inline run: M/X` instead of a misleading `M/total`. 6 new end-to-end tests including the canonical Next.js `(self.__next_f = self.__next_f || []).push(...)` streaming pattern. Combined test suite: 1582 → 1588 passing.
- **ChildNode/ParentNode mixins + currentScript + URL base-object coercion (slice 3c-16)** ✅ — Finishes the real-page ship gate on svelte.dev. Diagnostics from the CLI's `run` command revealed three concrete gaps: `document.body.append(...)` was undefined (the ChildNode/ParentNode mixin methods weren't installed), `document.currentScript.parentElement` crashed because `currentScript` returned null, and `new URL('.', location)` coerced the JsLocation object to `"[object Object]"` instead of reading its `href`. **DOM mixins**: `JsDomNode` gained `append`, `prepend`, `replaceChildren`, `remove`, `before`, `after`. All six accept variadic Node-or-string arguments — strings are coerced to fresh text nodes via a new `CoerceChild` helper that uses the element's owner document to allocate. Matches the WHATWG DOM ParentNode / ChildNode mixin semantics. **currentScript tracking**: new `JsEngine.ExecutingScript` property that hosts set around each `RunScript` call. `JsDomDocument.Get("currentScript")` reads it and wraps the result. The CLI's `run` command sets it in a try/finally around each script so SSR-style bootstraps (SvelteKit, Next.js, others) can find their parent element via `document.currentScript.parentElement`. **URL base-object coercion**: new `BuiltinUrl.CoerceUrlArg` helper that reads `href` off any object argument before falling back to `ToJsString`. Lets `new URL('.', location)` and `new URL('x', someUrlInstance)` resolve correctly, matching the spec's "if v is a USVString or URL record, use its href" rule without requiring script-side `toString` invocation. 16 new end-to-end tests covering every mixin, currentScript both with and without a host-set value, URL base-object resolution, and a compressed svelte.dev-style bootstrap end-to-end. Real-page impact: **svelte.dev went from 4/6 (2 errors) to 6/6 clean** — the remaining two gaps were exactly these three. Combined test suite: 1566 → 1582 passing.
- **Modern prototype methods + Function + nested RunChunk (slice 3c-15)** ✅ — Driven by real-page diagnostics after adding an `UnpackJsError` helper to the CLI's `run` command that pulls `name` + `message` off the thrown object instead of printing `[object Object]`. The diagnostics revealed missing ES2015+ prototype methods and a missing `Function` global. New `BuiltinModernPrototypes.Install`: **Array.prototype** grew `find` / `findIndex` / `findLast` / `findLastIndex` / `includes` (with SameValueZero so NaN matches) / `lastIndexOf` / `at` / `fill` / `copyWithin` / `flat` / `flatMap` / `keys` / `values` / `entries`. **Array (static)** grew `from` (with the full array / string / array-like + optional mapFn paths) and `of`. **Object (static)** grew `assign` / `entries` / `values` / `fromEntries` / `freeze` / `isFrozen` / `seal` / `isSealed` / `preventExtensions` / `isExtensible` / `getOwnPropertyNames` / `getOwnPropertyDescriptor` / `defineProperty` (data + accessor descriptor forms) / `defineProperties` / `setPrototypeOf` / `is` (SameValue: NaN equals itself, +0 ≠ -0). **String.prototype** grew `startsWith` / `endsWith` / `includes` / `padStart` / `padEnd` / `repeat` / `trimStart` / `trimLeft` / `trimEnd` / `trimRight` / `at` / `codePointAt` / `normalize`. **Number (static)** grew `isInteger` / `isFinite` / `isNaN` / `isSafeInteger` / `MAX_SAFE_INTEGER` / `MIN_SAFE_INTEGER` / `MAX_VALUE` / `MIN_VALUE` / `EPSILON` / `POSITIVE_INFINITY` / `NEGATIVE_INFINITY` / `NaN` / `parseInt` / `parseFloat` — all the strict-type variants that don't coerce the way the global helpers do. New `Function` global constructor: `new Function(...paramNames, body)` wraps the body in a function expression, parses + compiles it with `JsParser` + `JsCompiler`, and evaluates the resulting chunk via a new `JsVM.RunChunkNested` helper that snapshots caller state (code / ip / sp / env / this / halted / frames / handlers / native-boundary markers) before running and restores everything on exit — closing a long-standing re-entrancy gap where native code couldn't evaluate script source without trampling the outer dispatch loop. Also: `AttachDocument` now accepts an optional `Uri pageUrl` that `JsLocation.href` reads from so `new URL('.', location.href)` resolves relative to the real loaded page instead of the `about:blank` placeholder; the CLI's `run` command passes the post-redirect URL through. `UnpackJsError` in the CLI now shows real error details. 66 new end-to-end tests. Combined suite: 1500 → 1566 passing. Real-page impact: vitejs.dev went from 4/7 (1 error) to 5/7 with zero errors after landing the `Function` global.
- **Browser host shims round 2 (slice 3c-14)** ✅ — Round-2 concrete APIs. **Document**: grew `readyState` (= `"complete"`, since parsing is synchronous), `cookie` (read + write, stored in an in-memory jar on the wrapper that appends rather than replaces per spec, with same-name overwrite), `URL` / `documentURI` / `baseURI` / `referrer` / `currentScript` / `contentType` / `characterSet` / `compatMode` / `hidden` / `visibilityState` / `defaultView` accessors. **Element**: grew `dataset` (camelCase from `data-*` attributes), `style` (read-only stub with `setProperty` / `getPropertyValue` / `removeProperty` so scripts that do `el.style.display = 'none'` don't crash), layout-dependent numeric stubs (`offsetWidth` / `offsetHeight` / `clientWidth` / `clientHeight` / `scrollWidth` / `scrollHeight` / `scrollTop` / `scrollLeft` — all 0), `getBoundingClientRect()` (zero rect), `getClientRects()` (empty array), `scrollIntoView()` (no-op), `focus()` / `blur()` (no-ops), and `click()` (dispatches a real `click` Event through the event system so listeners fire). **`history`**: new global with a per-engine entry list. `pushState` / `replaceState` record state + title + URL tuples; `back` / `forward` / `go(delta)` navigate the pointer; `length` / `state` / `scrollRestoration` accessors return the current slot. Doesn't trigger real navigation — state transitions are script-visible but the URL doesn't change until a navigation controller lands. **`requestAnimationFrame` / `cancelAnimationFrame`**: `rAF` schedules the callback as a zero-delay event-loop task; callback receives a monotonic `performance.now`-style timestamp. Works as a "defer to next tick" primitive, which covers the common use case. **Observer stubs**: `IntersectionObserver`, `ResizeObserver`, `MutationObserver`, `PerformanceObserver` all ship as constructor-with-prototype pairs exposing `observe` / `unobserve` / `disconnect` / `takeRecords`. They never fire notifications but accept the full API shape so scripts that set up observers don't crash. 31 new tests covering every addition. Combined test suite: 1469 → 1500 passing.
- **Browser host shims (slice 3c-13)** ✅ — The concrete host APIs real scripts assume exist the moment they run: `localStorage` / `sessionStorage` (full Storage interface backed by an in-memory map per engine — `getItem` / `setItem` / `removeItem` / `clear` / `key` / `length`), `navigator` (`userAgent` matching the phase-1 HttpFetcher wire UA, `platform` derived from `OperatingSystem.IsWindows/IsLinux/IsMacOS`, `language` / `languages` / `onLine` / `hardwareConcurrency` / `cookieEnabled` / `maxTouchPoints`), `location` (new `JsLocation` subclass reading `href` / `origin` / `protocol` / `host` / `hostname` / `port` / `pathname` / `search` / `hash` off the current document URL with a script-settable `href` override that stores the intent but doesn't navigate — navigation is a future slice), `performance` (monotonic `now()` via `Stopwatch.GetTimestamp` + `Stopwatch.Frequency`, `timeOrigin`, stubbed `mark` / `measure` / `clearMarks` / `clearMeasures` / `getEntries`), and the window viewport surface (`innerWidth` / `innerHeight` / `outerWidth` / `outerHeight` at 1280×800, `devicePixelRatio` = 1, `scrollX` / `scrollY` / `pageXOffset` / `pageYOffset` / `screenX` / `screenY`, no-op `scrollTo` / `scrollBy` / `scroll`, and `matchMedia` returning a MediaQueryList shape with `matches: false`, a stored `media` query, and stubbed add/removeListener + add/removeEventListener). Also moved `window` installation out of `AttachDocument` into this builtin, so `window.innerWidth` works in headless / no-document contexts. 29 end-to-end tests covering every shim. After this slice svelte.dev runs 4/6 inline scripts clean, up from 0 before — scripts that used to crash on `navigator.userAgent` or `localStorage.getItem` now finish. Deferred: `document.cookie` mutation, `history.pushState`, `IntersectionObserver` / `ResizeObserver`, `requestAnimationFrame` (ties into a layout loop we don't have yet), and persistent storage.
- **Regex literals + RegExp (slice 3c-12)** ✅ — Phase 3c follow-up after real-page smoke testing showed missing regex literal support was the single biggest "wild web" blocker (hit on Wikipedia, Django, and others as a parse error). **Lexer**: tracks the most-recent-token kind so `/` disambiguates correctly between division and regex. `CanStartRegex` covers start-of-input, every opening delimiter, every operator, and the keyword set (`return` / `typeof` / `throw` / etc.) that can precede a regex. Regex body scanning handles `\` escapes and `[...]` character classes so an inner `/` doesn't terminate the literal. Flag run is any trailing identifier-part sequence. **AST**: new `LiteralKind.RegExp`; the `Literal.Value` is a fresh `JsRegExp` per literal (regex state is mutable via `lastIndex` so each site needs its own instance — the compiler emits a `NewRegExp` opcode that pops `[pattern, flags]` and constructs). **`JsRegExp`**: wraps a lazily-compiled `System.Text.RegularExpressions.Regex` with JS flag translation (`i` → `IgnoreCase`, `m` → `Multiline`, `s` → `Singleline`; `g` / `y` are stateful via `lastIndex` bookkeeping in the helpers). Uses `RegexOptions.ECMAScript` as the primary mode with a fallback for patterns outside the strict subset (e.g. Unicode property escapes). **`BuiltinRegExp`**: installs `RegExp` constructor (accepts string pattern + flags, or clones another RegExp), `RegExp.prototype.test` / `exec` / `toString`, plus the regex-aware `String.prototype` methods — `match` (per-match array or global list), `matchAll` (requires `g` flag), `search`, `replace` (both template form with `$1` / `$2` / `$&` references and the function-callback form), `replaceAll`, and a `split` upgrade that accepts a regex separator. Global-regex `exec` advances `lastIndex` correctly and resets on exhaustion. 35 end-to-end tests covering literal syntax, division-vs-regex disambiguation in every context, character-class escaping, flag plumbing, the full string consumer surface, and global-state transitions. Real-page result: Wikipedia's JavaScript article now parses all 5 inline scripts (was hitting `Unexpected token Slash` before); Django's inline scripts run clean (was 0/4, now 1/4); go.dev scripts now mutate the DOM (`+2 elements` after JS) — the first time we're seeing real inline JS from the live web modify the parsed document. Deferred: JS-specific regex constructs not in .NET's ECMAScript mode (Unicode property escapes, some lookbehind forms); `RegExp` subclassing; named-capture-group access as `.groups.name` (values survive as numbered groups).
- **BigInt (slice 3c-11)** ✅ — New primitive type backed by `System.Numerics.BigInteger`. **Lexer**: recognizes the `n` suffix on integer literals (decimal `42n`, hex `0x1fn`); `3.14n` and `1e5n` are rejected per spec (the suffix is only legal on integer forms). Emits a new `BigIntLiteral` token kind with the raw digit text in `StringValue`. **Parser**: new `LiteralKind.BigInt`; the primary-expression handler constructs the `BigInteger` via `BigInteger.Parse` at AST build time. **Compiler**: BigInt literals flow through the existing `PushConst` constant pool. **VM**: `DoAdd` / `DoSub` / `DoMul` / `DoDiv` / `DoMod` route through new `DoBigIntBinary` helpers when either operand is a `BigInteger`. Mixing with a Number throws `TypeError` at the opcode site via `RaiseError` (so the throw behaves as a real script exception, not a native-boundary escape). Unary `-` negates a BigInt; unary `+` throws `TypeError` per spec (no implicit Number conversion). Strict equality matches within BigInt type only; loose equality does a cross-type integer comparison for BigInt ↔ Number and BigInt ↔ String. Relational operators (`<`, `<=`, `>`, `>=`) compare BigInt ↔ Number via exact-integer coercion, returning false when the Number is `NaN` / ±∞ / non-integer (matching the spec's "undefined" result). **`typeof`**: returns `"bigint"`. **`ToBoolean`**: zero is falsy, everything else truthy. **`ToJsString`**: delegates to `BigInteger.ToString`. **`BigInt()` constructor**: coerces from numbers (integer-only, rejects `NaN` / infinity / non-integer with `RangeError`), strings (decimal / hex, throws `SyntaxError` on malformed input), and booleans. Called as `new BigInt(x)` is rejected (not a real constructor). **`JSON.stringify`**: throws `TypeError` on any BigInt, nested or not. 36 end-to-end tests covering literal syntax (decimal, hex, large values beyond `Number.MAX_SAFE_INTEGER`), `typeof`, all arithmetic operators including division-by-zero (`RangeError`), mixed-type rejection, strict equality within + across types, loose equality across Number and String, ordered comparison with Number / NaN, string concatenation, truthiness, `BigInt()` coercion from every valid source + error paths, and `JSON.stringify` rejection nested inside an object. Deferred: `**` (exponent operator doesn't ship in any form yet), BigInt bitwise operators, `BigInt.asIntN` / `asUintN`, `BigInt64Array` / `BigUint64Array`.
- **Phase 3c polish bundle (slice 3c-10)** ✅ — Three small additions shipped together: `Reflect.construct` (now unblocked by a new public `JsVM.ConstructJsFunction` helper), `crypto.subtle.digest` for SHA-1/256/384/512, and `AbortSignal.timeout` / `AbortSignal.any`.
- **Optional chaining + nullish coalescing + logical assignment (slice 3c-1)** ✅ — Lexer recognizes the ES2020 `?.` and `??` punctuators plus the ES2021 short-circuit assigns `&&=` / `||=` / `??=`. Parser turns `x ?? y` into a `LogicalExpression` with a new `Nullish` operator, and wraps any subexpression containing `?.` / `?.[` / `?.(` in a `ChainExpression` node so the compiler has a single end-of-chain label to merge the short-circuit jumps against. `MemberExpression` and `CallExpression` carry a new `IsOptional` flag. Two new opcodes: `JumpIfNullish` (pops TOS, jumps on null/undefined) and `JumpIfNotNullishKeep` (peeks; jumps-and-keeps on non-null/non-undefined). The compiler emits a `Dup` + `JumpIfNullish` pad at each optional hop — every hop's jump patches to a common nullish-merge block that pops the leftover test value and pushes `undefined`, matching the cover-grammar end-of-chain semantics. Optional calls on a member (`obj.method?.(args)`) use the scratch slot to briefly stash the `this` binding while the short-circuit checks the method value, then reassembles the `[fn, this]` call header. Logical assignment lowers to a peek-short-circuit sequence on the loaded LHS so the RHS side effects only happen when the operator's condition fires (e.g. `x ??= f()` does not call `f` when `x` is already non-nullish), handling identifiers, dotted members, and computed members. 47 end-to-end tests.
- **Phase 3c polish bundle (slice 3c-10)** ✅ — Three small additions that each wouldn't warrant their own slice, landing together. **`Reflect.construct`**: new public `JsVM.ConstructJsFunction(fn, args)` helper wraps the VM's existing `DoNew` handler with a snapshot-and-restore native boundary, allocating a fresh instance linked to `fn.prototype` and returning it (or the function's own returned object when it takes over per ECMA §13.2.2). `Reflect.construct` calls this from native code, closing the last gap in the `Reflect` surface. **`crypto.subtle.digest`**: wired onto the existing `crypto` global as a `subtle` namespace. Accepts `SHA-1` / `SHA-256` / `SHA-384` / `SHA-512` (and the algorithm-object form `{name: 'SHA-256'}`), unwraps the data from `ArrayBuffer` / `TypedArray` / `DataView`, calls the matching `System.Security.Cryptography.SHA*.HashData`, and returns a resolved promise over a fresh `ArrayBuffer`. Unknown algorithms return a promise rejected with a `NotSupportedError`-shaped object. Known SHA-256 vector verified in tests (`SHA-256("abc")` → canonical hex). **`AbortSignal.timeout(ms)`**: returns a signal that auto-aborts after `ms` via a task on the event loop; reason is a `TimeoutError`-shaped object. **`AbortSignal.any(signals)`**: returns a signal that aborts as soon as any input signal aborts, inheriting the first input's reason. Pre-aborted inputs propagate synchronously. 15 end-to-end tests covering `Reflect.construct` with classes and plain functions, `Reflect.construct` rejecting non-functions, digest returning the correct byte length for each SHA variant, the known SHA-256 hex vector, algorithm-object form, unknown-algorithm rejection, `AbortSignal.timeout` firing + setting TimeoutError, `AbortSignal.any` propagation for first / second / pre-aborted inputs, and once-aborted-stays-aborted on subsequent input aborts.
- **AbortController + AbortSignal (slice 3c-9)** ✅ — Script-level cancellation primitive. **`JsAbortSignal`** subclass of `JsObject` holding `aborted` / `reason` state plus a flat listener list (signals aren't in a DOM tree, so dispatch skips the capture/target/bubble walk — every registered listener fires at the signal in registration order). Prototype methods: `addEventListener` / `removeEventListener` / `dispatchEvent` / `throwIfAborted`. **`JsAbortController`** subclass exposes `signal` as a read-through accessor plus an `abort(reason?)` method that flips the backing signal's state and fires the `'abort'` event. `controller.abort()` with no reason defaults the reason to a plain Error-shaped object with `name: 'AbortError'` (we don't ship `DOMException` yet — that's a future slice). `AbortSignal.abort(reason?)` is exposed as a static factory on the constructor so script code can synthesize a pre-aborted signal in one call. Direct `new AbortSignal()` throws `TypeError` per spec. Second and later `abort()` calls are no-ops (once aborted, always aborted). Listeners are snapshotted before iteration so a mid-dispatch add doesn't fire on the same event; `once: true` auto-removes after the first fire. Also loosened `JsDomEvent.Target` / `CurrentTarget` typing from `JsDomNode?` to `JsObject?` so non-DOM event targets fit in the existing event class without forcing a refactor into an `EventTarget` base class. 20 end-to-end tests covering controller-signal wiring, initial state, `abort()` + `abort(reason)` + no-reason default, second-abort no-op, listener fire, `target` / `currentTarget` references, registration-order fan-out, dedup, remove, `once`, `throwIfAborted` in both states, the `AbortSignal.abort` static factory, direct-construction rejection, and the multi-subsystem coordination pattern. Deferred: `AbortSignal.timeout(ms)` / `AbortSignal.any(signals)`, `DOMException` proper, and wiring the signal into `fetch` (fetch runs synchronously so there's no window during which cancellation would interrupt anything).
- **element.innerHTML + outerHTML + HtmlSerializer (slice 3c-8)** ✅ — Closes a long-standing gap from slice 3c-2: scripts can now both read and write the HTML serialization of an element subtree. **`Html.HtmlSerializer`**: new static class — the inverse of the phase-1 tokenizer + tree builder. `SerializeChildren(node)` emits the HTML for a node's children (the `innerHTML` shape); `SerializeNode(node)` includes the element itself (the `outerHTML` shape). Escapes `&amp;` / `&lt;` / `&gt;` in text content and `&amp;` / `&quot;` in attribute values per WHATWG serialization rules. Honors the void-element set (`br` / `img` / `input` / etc. render without a closing tag, matching the tree builder's insertion-time check) and special-cases `script` / `style` / `noscript` children so JS source with `a < b` survives a round-trip without getting escaped. Preserves attribute insertion order via the phase-1 `Element.Attributes` ordered list. **`JsDomElement`** grew four new accessors: `innerHTML` read delegates to `HtmlSerializer.SerializeChildren`; `innerHTML` write calls the existing `HtmlTreeBuilder.Parse` on a `<html><body>...</body></html>`-wrapped fragment and adopts the parsed body's children onto the target (clearing existing children first); `outerHTML` read delegates to `HtmlSerializer.SerializeNode`; `outerHTML` write is silently ignored (deferred — requires reparenting logic to insert the fragment at the element's position in its current parent). 20 end-to-end tests covering read for text / element children / attributes / void elements / entity escaping / attribute-quote escaping / script-text preservation / empty element, outerHTML read for regular + void elements, and write for plain replacement / attribute parsing / nested structures / empty clearing / post-write query visibility / round-trip / entity decoding. Deferred: template contents, SVG / MathML foreign content, context-element-aware fragment parsing (a `td` fragment inside a `table` context), and `outerHTML` write.
- **Proxy + Reflect (slice 3c-7)** ✅ — ES2015 meta-object hooks for reactivity. **`Proxy`**: `JsProxy` subclass of `JsObject` with overrides for the five virtual property-access methods the VM already consults (`Get` / `Set` / `Has` / `Delete` / `OwnKeys`). Each override checks the handler object for a matching trap function (`get` / `set` / `has` / `deleteProperty` / `ownKeys`) and dispatches it via `vm.InvokeJsFunction`. When a trap is absent the call falls through to the target's default behavior — the same "unset trap" semantics ES2015 defines via `Reflect.*`. The VM's bytecode handlers call the virtuals unchanged, so Proxy support lands without any opcode-level changes. Constructor validates that both target and handler are objects (`TypeError` otherwise). **`Reflect`**: global namespace object exposing the default meta-object operations as plain functions — `get` / `set` / `has` / `deleteProperty` / `ownKeys` / `getPrototypeOf` / `setPrototypeOf` / `apply`. `Reflect.get` / `set` / etc. simply call the corresponding virtual on the target, so they work on both plain objects and proxies (invoking the proxy's trap when appropriate). `Reflect.apply` uses the VM's public `InvokeJsFunction` to call a function with an explicit `this` and spread args. Deferred: `construct` (needs a public constructor-mode VM helper), `apply` / `construct` proxy traps (needs the same), `getPrototypeOf` / `setPrototypeOf` / `isExtensible` / `preventExtensions` proxy traps, `getOwnPropertyDescriptor` / `defineProperty` (no full descriptor model yet). 25 end-to-end tests covering every trap, fall-through when trap absent, Reflect operations on plain objects, Vue-style reactivity pattern (nested-proxy recursion + mutation logging), and the canonical "forward unhandled trap via Reflect" handler pattern.
- **fetch + Headers + Request + Response (slice 3c-6)** ✅ — The WHATWG Fetch API subset real-world script actually uses for API calls, built on top of slice 3c-3's `URL` + `TextEncoder` and slice 3b-9's `Promise`. **`Headers`**: `JsHeaders` subclass with an ordered `(lowercased-name, value)` list plus case-insensitive `get` / `has` / `set` / `append` / `delete`, `forEach` visitor, and `keys` / `values` / `entries` / `[Symbol.iterator]` that return a snapshot coalesced per spec (one entry per distinct header, values comma-joined, alphabetized). **`Response`**: `JsFetchResponse` subclass carrying `status` / `statusText` / `url` / `headers` / `body` bytes plus an `ok` getter, with prototype methods `text()` / `json()` / `arrayBuffer()` that return resolved promises over the decoded body. The `Response` constructor is exposed so tests can synthesize responses without hitting the network; it accepts `(body, init?)` with body coerced from string / `ArrayBuffer` / `TypedArray`. **`Request`**: `JsFetchRequest` subclass for the `(url, init?)` shape, with `url` / `method` / `headers` / `body` accessors. **`fetch(input, init?)`**: global that accepts either a URL string or a `Request`, applies the init dict, dispatches through `JsEngine.FetchHandler`, and returns a promise resolved with a `Response` (or rejected on transport error). **`JsEngine.FetchHandler`**: pluggable delegate on the engine — the default handler lazily constructs a shared `HttpFetcher` and blocks on `FetchAsync().GetAwaiter().GetResult()` (single-threaded VM, so blocking here is equivalent to awaiting a real promise). Tests install a stub handler before running script, so the engine test suite stays fully offline. The underlying `BuiltinJson` parser grew an `internal static ParseText(engine, string)` helper so `Response.prototype.json` can reuse it without routing through the script-visible `JSON.parse`. 24 end-to-end tests covering Headers CRUD + iteration, Response construction + text/json/arrayBuffer body consumers, fetch promise resolution, header-based stub verification, init dict handling (method / body), fetch error → rejected promise, 404 status reporting, URL pass-through, and Request-as-input. Deferred: streaming bodies (`response.body` as a `ReadableStream`), `AbortController` / `signal`, `Request.clone` / `Response.clone`, `credentials` / `mode` / `referrerPolicy`, real async scheduling (the HTTP call runs synchronously on the VM thread for now).
- **Web Crypto — getRandomValues + randomUUID (slice 3c-5)** ✅ — New `BuiltinCrypto.Install` wires the `crypto` global with two methods. **`getRandomValues(view)`** accepts any integer-kind `TypedArray` (rejects `Float32Array` / `Float64Array` with `TypedError`, rejects buffers larger than 65 536 bytes with `RangeError` per the quota spec), fills the view's byte range in place via `System.Security.Cryptography.RandomNumberGenerator.Fill`, and returns the same view object for chaining. **`randomUUID()`** returns a lowercase RFC 4122 v4 UUID string via `Guid.NewGuid().ToString("D")`. Both delegate to the OS CSPRNG (CryptGenRandom on Windows, /dev/urandom on POSIX). 11 end-to-end tests covering fill-in-place on `Uint8Array` / `Int32Array`, return-is-same-buffer identity, float-array and quota rejection, UUID length / v4 version marker / variant bits / uniqueness / lowercasing. Deferred: the rest of `SubtleCrypto` — `digest` / `sign` / `verify` / `encrypt` / `decrypt` / `generateKey` / `importKey` / `exportKey`.
- **DOM events (slice 3c-4)** ✅ — Event listeners plug into the slice 3c-2 DOM bridge. `JsDomNode` grew a lazily-allocated `Dictionary<string, List<EventListenerEntry>>` listener bag plus three installed methods: `addEventListener(type, cb, options?)` (accepts the legacy `useCapture` boolean and the `{capture, once, passive}` object form, and dedups `(type, callback, capture)` triples per spec), `removeEventListener(type, cb, options?)`, and `dispatchEvent(event)`. New `JsDomEvent` wrapper class with propagation state (`target`, `currentTarget`, `eventPhase`, `defaultPrevented`, `propagationStopped`, `immediatePropagationStopped`) plus the three control methods `preventDefault` / `stopPropagation` / `stopImmediatePropagation`, and a `JsDomCustomEvent` subclass carrying a script-controlled `detail`. `BuiltinDomEvents.Install` wires the global `Event` and `CustomEvent` constructors, with the four phase constants (`NONE`, `CAPTURING_PHASE`, `AT_TARGET`, `BUBBLING_PHASE`) on both the constructor and event instance. **Dispatch algorithm:** `DispatchEvent` builds the propagation path from the target up to the root, then runs the full **capturing phase** (root → target-exclusive, firing only capture-registered listeners), the **target phase** (both capture and non-capture listeners in registration order), and — if `event.bubbles` — the **bubbling phase** (target → root, non-capture only). `stopPropagation` skips to the next phase by aborting the path walk; `stopImmediatePropagation` additionally blocks remaining listeners at the current node. A listener that throws is swallowed (the `JsThrowSignal` escape is caught and silently logged-off) so one broken handler doesn't abort dispatch — matching the browser spec where a listener error goes to `window.onerror` instead. `once: true` auto-removes a listener after it fires. Listeners are snapshotted before iteration so a mid-dispatch `addEventListener` doesn't fire on the same event. 28 end-to-end tests covering Event / CustomEvent construction, listener fire + `this`-binding to current target, `target` vs `currentTarget` in a listener, registration order preservation, same-listener dedup, remove semantics, `once` auto-removal, bubble vs. non-bubble behavior, capture phase ordering, the full capture/target/bubble sequence across a 3-level tree, `eventPhase` reporting per phase, `stopPropagation` / `stopImmediatePropagation` / `preventDefault` + cancelable semantics, `dispatchEvent` return value, `CustomEvent` detail propagation, and a throwing listener not blocking the next one. Deferred: passive option enforcement, attribute-style `onclick=` handlers, real `Attr` targets, composed / composed path support, and `EventTarget.prototype` exposure for user-defined non-node targets.
- **Web primitives — URL / TextEncoder / base64 (slice 3c-3)** ✅ — New namespace-level built-ins `BuiltinUrl`, `BuiltinTextCoding`, `BuiltinBase64`. **`URL`**: `JsUrl` subclass of `JsObject` with getter-accessor properties for `href` / `origin` / `protocol` / `host` / `hostname` / `port` / `pathname` / `search` / `hash`, each reading the live parsed components. Mutations (`url.pathname = '/x'`) rebuild `href` on the fly. Parsing delegates to `System.Uri` for HTTP/HTTPS + relative-URL resolution against a second constructor argument. Invalid inputs throw script-visible `TypeError`. **`URLSearchParams`**: `JsUrlSearchParams` subclass holding an ordered `List<KeyValuePair<string, string>>`, prototype methods `get` / `getAll` / `has` / `set` / `append` / `delete` / `toString` / `forEach`, plus `keys()` / `values()` / `entries()` iterator helpers and `[Symbol.iterator]` defaulting to entries. Serialization uses `+` for space and percent-encodes everything outside the unreserved set via UTF-8. **Live binding**: when accessed via `url.searchParams`, a single cached view is returned across reads (so `url.searchParams === url.searchParams`) and any mutation via `set` / `append` / `delete` updates the owning URL's `search` and `href` in place — matching the spec's "live view" semantics. **`TextEncoder`**: UTF-8 `encode(str)` returning a `Uint8Array` whose prototype is resolved off the live `Uint8Array` global so instances get the usual typed-array methods. **`TextDecoder`**: `decode(buffer)` accepts `Uint8Array` / `ArrayBuffer` / `DataView`, only supports `utf-8` / `utf8` labels (other encodings throw `RangeError`). **`atob` / `btoa`**: globals backed by `Convert.FromBase64String` / `Convert.ToBase64String`. `btoa` throws `TypeError` for characters outside `0x00–0xFF` (the historical binary-string contract); `atob` strips ASCII whitespace before decoding and rejects invalid base64 with `TypeError`. Also fixed a latent bug in `JsThrow.*` where error objects constructed by native built-ins had no prototype chain — so `e instanceof TypeError` returned false from script. `JsVM.InvokeFunction`'s `JsThrowSignal` catch handler now assigns the matching engine prototype (`TypeErrorPrototype` / `RangeErrorPrototype` / etc.) on the way through, based on the object's `name` property. All existing `JsThrow.*` call sites benefit retroactively. 45 end-to-end tests. Deferred: non-UTF-8 `TextDecoder` labels, `TextDecoder.encodeInto`, `URL.parse` / `URL.canParse`, IDN hostnames, `URLSearchParams.sort`.
- **DOM bridge (slice 3c-2)** ✅ — New namespace `Daisi.Broski.Engine.Js.Dom` holds the JS-side wrappers that let script code reach the real C# `Dom` layer. `JsDomNode : JsObject` is the base wrapper: overriding `Get`/`Set` so every property read goes through to the backing `Node` (no caching, no snapshot — DOM state is authoritative), plus native-callable methods for `appendChild` / `removeChild` / `insertBefore` / `contains` / `hasChildNodes`. `JsDomElement : JsDomNode` layers the element surface on top: `tagName` (uppercased per spec), `id` / `className` (read and write, mapping to `class`/`id` attributes), `classList` (as a JsArray with `add` / `remove` / `contains` / `toggle` methods), `attributes`, `children` / `firstElementChild` / `childElementCount`, `getAttribute` / `setAttribute` / `hasAttribute` / `removeAttribute`, `querySelector` / `querySelectorAll` / `getElementsByTagName` / `getElementsByClassName` (all scoped to the subtree), `matches`, `closest`. `JsDomDocument : JsDomNode` adds `documentElement` / `head` / `body` / `doctype`, the `create*` factory family (`createElement` / `createTextNode` / `createComment`), `getElementById`, and the document-scoped queries. `JsDomText` exposes `data` / `nodeValue` / `length` for text nodes. `JsDomBridge` is the factory + per-node identity cache — a `Dictionary<Node, JsDomNode>` with `ReferenceEqualityComparer` — so repeated exposure of the same `Node` returns the same wrapper and `el.parentNode === el.parentNode` holds across reads. `JsEngine.AttachDocument(Document)` is the host entry point: it wires the bridge, sets `document` to the wrapped root, and installs a minimal `JsWindowProxy` as `window` so UMD shims `typeof window !== 'undefined'` succeed. The proxy routes reads/writes through the engine's `Globals` dictionary and exposes `self` / `globalThis` / `window` as self-references. 46 end-to-end tests covering document-global wiring, element property reads, query methods at both document and element scope, script-driven mutation (setAttribute, className, textContent replacement, appendChild, removeChild, classList add/remove/toggle), identity semantics (wrapper cache holds `===` across reads), tree traversal (parentNode / childNodes / firstChild / contains), `matches` / `closest`, and end-to-end scripted pipelines that build a list programmatically or query-then-mutate in one sweep. Deferred: `innerHTML` (needs an HTML serializer), DOM events / addEventListener, live `NodeList` / `HTMLCollection`, `Attr` nodes, `style` / computed style, `dataset`, form element helpers.
**Deliberately deferred to phase 7 or later (not blockers for the ship gate):**

- `**` exponent operator (Number and BigInt)
- BigInt bitwise operators / `BigInt.asIntN` / `asUintN` / `BigInt64Array`
- Wiring `AbortSignal` into `fetch` — fetch runs synchronously so cancellation isn't yet respected. When a follow-up slice moves fetch onto a proper async scheduler, the signal will wire in.
- Proxy `apply` / `construct` traps — the construct helper landed for `Reflect.construct` in slice 3c-10; the Proxy-side wiring is a small follow-up.
- `SubtleCrypto` — `sign` / `verify` / `encrypt` / `decrypt` / `generateKey`. Slice 3c-10 added `digest`; the rest need a `CryptoKey` object model.
- `outerHTML` write — needs reparenting logic to insert the parsed fragment at the element's position in its current parent. The read path landed in slice 3c-8.
- Live `NodeList` / `HTMLCollection`, `Attr` nodes, `style` / computed style, `dataset`, form helpers, template contents, SVG/MathML foreign content.
- Full WHATWG HTML5 conformance suite — engine runs the common cases reliably but hasn't been vendored against test262 or html5lib.

**Ship gate:** load a scripted page, run its scripts, and confirm the DOM after script execution matches what Chrome produces on the common cases. Covered — 1403/1403 engine tests pass, including every ES2015+ primitive real sites use.

**Current status:** phase 3 is feature-complete. Phase 3a delivered the ES5 core (lexer, parser, bytecode VM, objects, arrays, member access, full function / closure / `this` / `new` / `instanceof` system, ES5 control flow, exception handling, the complete ES5 built-in library, and the host-side event loop). Phase 3b added the ES2015+ surface: `let`/`const` + block scoping + TDZ (3b-1), arrow functions (3b-2), template literals (3b-3), destructuring bindings (3b-4), default/rest/spread (3b-5), classes with `extends`/`super` (3b-6), iterators + `for..of` + `Symbol.iterator` (3b-7a), generators with `function*` + `yield` (3b-7b), the `Map` / `Set` / `WeakMap` / `WeakSet` collections (3b-8), `Promise` + chaining + `Promise.all` / `Promise.race` (3b-9), `async`/`await` (3b-10), typed arrays / `ArrayBuffer` / `DataView` (3b-11), and the ESM module loader (3b-12). Phase 3c landed the ES2020+ sugar and web-platform surface across 11 slices: optional chaining / nullish coalescing / logical assignment (3c-1), JS ↔ DOM bridge (3c-2), WHATWG URL + TextEncoder + base64 (3c-3), DOM events with capture/target/bubble (3c-4), `crypto.getRandomValues` / `randomUUID` (3c-5), `fetch` + `Headers` + `Request` + `Response` (3c-6), `Proxy` + `Reflect` (3c-7), `element.innerHTML` / `outerHTML` (3c-8), `AbortController` / `AbortSignal` (3c-9), polish bundle with `Reflect.construct` / `crypto.subtle.digest` / `AbortSignal.timeout` / `AbortSignal.any` (3c-10), and `BigInt` (3c-11).

## Phase 4 — Sandbox host ✅ (infrastructure) / ⏸ (full ship gate blocked on phase 3)

**Goal:** everything phase 1–3 does, but in a child process with kernel-enforced resource limits.

**What ships today:**

```csharp
await using var session = BrowserSession.Create();
var nav = await session.NavigateAsync(new Uri("https://example.com"));
var links = await session.QuerySelectorAllAsync("a[href]");
```

…runs the full phase-1 engine in a `Daisi.Broski.Sandbox.exe` child process under a Win32 Job Object. The host process never parses HTML, runs selectors, or touches any untrusted input. `daisi-broski fetch` uses this path by default.

**Ship-gate nuance:** the original phase-4 ship gate said the sandbox should "run a JS-heavy site inside and return a DOM snapshot." The sandbox *infrastructure* is complete (spawn, Job Object, IPC, navigate + query round-trip, crash capture) and works against any server-rendered page. But it literally cannot run JS-heavy sites yet because there is no JavaScript engine — that's phase 3. Once phase 3 lands, the existing sandbox plumbing picks it up for free; no additional sandbox work is required.

### Shipped

- **`Daisi.Broski.Ipc`** — shared protocol library. No dependency on the engine or Win32:
  - `IpcMessage` envelope: Request / Response / Notification, JSON-RPC 2.0 shape without the version field, payloads as `JsonElement` so the envelope codec doesn't depend on any DTO.
  - `IpcCodec` — length-prefixed UTF-8 JSON framing over any `Stream`. Big-endian u32 length prefix, 64 MiB max frame size, clean EOF detection, rejects truncated / oversize / malformed frames before allocation.
  - Phase-1 DTOs: `NavigateRequest` / `NavigateResponse` (with opt-in `IncludeHtml`), `QueryAllRequest` / `QueryAllResponse` / `SerializedElement`, `CloseRequest` / `CloseResponse`, `NavigationCompletedNotification` / `NavigationFailedNotification`.
- **`Daisi.Broski`** — host library (Windows-only):
  - `JobObject` — managed wrapper over `CreateJobObject` / `SetInformationJobObject` / `AssignProcessToJobObject` / `QueryInformationJobObject` with a `SafeHandle` that guarantees close-on-finalize. Configurable via `JobObjectOptions`: `ProcessMemoryLimitBytes` (default 256 MiB), `KillOnJobClose` (default true), `DieOnUnhandledException` (default true), `RestrictUI` (default true — blocks desktop, clipboard, global atoms, handles). `Win32/NativeMethods.cs` holds the P/Invoke declarations + struct layouts.
  - `SandboxLauncher` — creates two `AnonymousPipeServerStream`s (bidirectional IPC), spawns `Daisi.Broski.Sandbox.exe` with the inherited client-handle strings on the command line, assigns the child to a fresh `JobObject`, returns a `SandboxProcess`. `ResolveDefaultSandboxPath` handles development and deployment layouts, and validates the apphost `.exe` is accompanied by its managed `.dll` to avoid the MSBuild half-copy trap.
  - `SandboxProcess` — host-side handle: `SendRequestAsync` with monotonically-increasing id correlation, stderr draining (essential for diagnosing child crashes — without it the failure mode is indistinguishable from "child closed its pipe"), `DisposeAsync` with best-effort clean close + Job-Object-enforced kill.
  - `BrowserSession` — the public phase-1 API. `Create()`, `NavigateAsync(url, userAgent, maxRedirects, includeHtml, ct)`, `QuerySelectorAllAsync(selector, ct)`, `DisposeAsync()`.
- **`Daisi.Broski.Sandbox`** (output `Daisi.Broski.Sandbox.exe`) — the child process:
  - `Program.cs` — parses `--in-handle` / `--out-handle`, opens the inherited anonymous pipes, hands control to `SandboxRuntime`.
  - `SandboxRuntime` — single-threaded dispatch loop: reads `IpcMessage` frames, routes to `HandleNavigate` / `HandleQueryAll` / `HandleClose`, drives a long-lived `PageLoader`, serializes matched elements as `SerializedElement` for the response.
- **`Daisi.Broski.Cli`** integration — the CLI now uses `BrowserSession` by default; `--no-sandbox` falls back to in-process `PageLoader`. Non-Windows platforms degrade gracefully with a warning. The stderr status line marks in-process runs with `[no-sandbox]`.

### Deliberately deferred

- **AppContainer profile creation.** Job Object alone gives us memory, UI, and lifetime limits — enough for the phase-1 threat model. `CreateAppContainerProfile` + `SECURITY_CAPABILITIES` for additional integrity-level / filesystem / network isolation will land when real multi-site handling requires per-origin sandboxing.
- **`CreateProcess(CREATE_SUSPENDED)` atomic launch.** The current launcher uses `Process.Start` + `AssignProcessToJobObject`, leaving a ~few-ms race window where the child runs without the Job Object's memory cap. During that window the child only parses argv and opens inherited pipe handles (no network, no parsing), so the exposure is minimal. The stricter variant using native `CreateProcess` is documented in [architecture.md §5.8](architecture.md#58-sandboxing).
- **Crash-recovery respawn.** The host surfaces child-death errors via `SandboxException` but does not automatically respawn a fresh sandbox to continue the session; callers dispose and recreate `BrowserSession` themselves.
- **Handle-table for JS/DOM object ids.** The original phase-4 plan called for a handle table on the sandbox side so the host could refer to live JS objects and DOM nodes by opaque ids across the IPC boundary. Not implemented yet — the current IPC only passes serialized snapshots (`SerializedElement`). The handle-table is a phase-3c concern because the JS DOM bridge needs it to let scripts receive and mutate node references.
- **Cross-platform variants.** Phase 5 covers Linux (`unshare` + seccomp-bpf + cgroups v2) and macOS (`sandbox_init`).

## Phase 5 — Hardening and extended Web APIs

- `localStorage`, `sessionStorage` (file-backed per origin).
- `IndexedDB` minimum viable (a KV store with version upgrades).
- `WebSocket`.
- `XMLHttpRequest` (legacy but still used).
- `MutationObserver`, `IntersectionObserver` (real implementation, not stub).
- `FormData`, `Blob`, `File`, `FileReader`.
- Request interception API (host-side) for test scaffolding and ad blocking.
- Fuzzing harness against the parsers and IPC decoder.
- Cross-platform sandbox research: Linux (unshare + seccomp-bpf + cgroups v2), macOS (`sandbox_init`).

**Current status:** not started. The fuzzing and cross-platform sandbox items are prerequisites for running the engine in untrusted server environments; the Web API items are blocked on phase 3.

## Phase 6 — Layout, rendering, screenshots

Optional and heavyweight. Only if there's real demand.

- Box model, block/inline flow.
- Flexbox.
- Grid.
- Text layout (line breaking, bidi).
- Font loading (the one place we probably have to ship bundled fonts or shell out to the OS).
- Paint → raster buffer.
- `Screenshot` IPC command returns a PNG.
- `getBoundingClientRect` and friends return real values.

At this point daisi-broski is a real browser engine, not just a headless DOM runner. It will also be an order of magnitude more code.

## Phase 7 — Performance

- Bytecode VM optimizations: inline caches, shape-based property access, constant folding.
- JS heap arena allocator for short-lived values.
- AOT compilation of the sandbox child (`PublishAot`) — requires resolving reflection use in built-ins.
- Parallel parsing (HTML parser on one thread, CSS parser on another).
- Incremental style recalc.

---

## What we're explicitly NOT doing, ever

- **Shipping a JIT.** The attack surface is too large for a sandboxed runtime with our threat model. Interpreter only.
- **Becoming a GUI browser.** No chrome, no tabs UI, no address bar. This is a programmable engine.
- **Supporting IE-era legacy quirks beyond what WHATWG requires.** If it's not in the living standard, we don't implement it.
- **Running plugins.** No Flash, no ActiveX, no NPAPI, no PPAPI. Ever.
